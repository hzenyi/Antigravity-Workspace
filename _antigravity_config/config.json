{
  "userSettings": {
    "artifactReviewMode": "ARTIFACT_REVIEW_MODE_TURBO",
    "autoExecutionPolicy": "CASCADE_COMMANDS_AUTO_EXECUTION_EAGER",
    "enableTerminalSandbox": false,
    "globalPermissionGrants": {
      "allow": [
        "command(powershell -Command \"\n$gitCmd = Get-Command git -ErrorAction SilentlyContinue\nif ($gitCmd) {\n    Write-Host '=== Git in PATH ==='\n    Write-Host ('Source: ' + $gitCmd.Source)\n    \u0026 git --version\n} else {\n    Write-Host '=== Git not found in PATH ==='\n}\n\nWrite-Host '`n=== Checking Common Locations ==='\n$locations = @(\n    '$env:ProgramFiles\\Git\\cmd\\git.exe',\n    '${env:ProgramFiles(x86)}\\Git\\cmd\\git.exe',\n    '$env:LOCALAPPDATA\\Programs\\Git\\cmd\\git.exe',\n    '$env:LOCALAPPDATA\\GitHubDesktop',\n    '$env:ProgramFiles\\TortoiseGit\\bin\\TortoiseGitProc.exe'\n)\nforeach ($loc in $locations) {\n    $expanded = $ExecutionContext.InvokeCommand.ExpandString($loc)\n    if (Test-Path $expanded) {\n        Write-Host ('Found: ' + $expanded)\n    }\n}\n\")",
        "command(npm)",
        "command(powershell)",
        "command(py)",
        "command(python)",
        "command(where)",
        "command(winget)",
        "command(git)",
        "command(git --version; Get-Command git-gui, gitk, gh -ErrorAction SilentlyContinue | Select-Object Name, Source)",
        "command(Test-Path \"C:\\Program Files\\Git\\bin\\bash.exe\"; Test-Path \"$env:LOCALAPPDATA\\GitHubDesktop\")",
        "command(git config --list --global)",
        "read_url(w3.sivs.chc.edu.tw)",
        "read_url(sivs.chc.edu.tw)"
      ]
    },
    "nonWorkspaceFileAccessPolicy": "AGENT_SETTING_POLICY_ALLOW",
    "remoteControlEnabled": true,
    "remoteControlHostname": "desktop-vvqm7qm-noble-halo",
    "themeMode": "THEME_MODE_INHERIT"
  }
}