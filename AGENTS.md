# 全域 AI Agent 協作規範 (Global AGENTS.md)

本文件為當前工作區（`91_Antigravity`）根目錄下所有專案與工作區之通用規範與總體治理總綱。
工作區全面劃分為三大核心領域：「01_行政」、「02_教學」、「03_程式」，各領域均設有專屬 `AGENTS.md` 規範細則。

---

## 1. 全域核心原則與目錄命名鐵律 (Global Principles & Naming Rules)

### 1.1 目錄與檔案命名繁體中文原則 ⭐⭐⭐
> [!IMPORTANT]
> **除 Antigravity 官方工具與開發環境強制規定之系統目錄外，所有專案、業務子目錄及未來新建立之資料夾，一律強制以「繁體中文」命名。**
> 
> - **系統保留目錄（維持英文/系統規定）**：
>   - `.agents/`（Antigravity 官方 Workspace 技能目錄）
>   - `_antigravity_config/`（Antigravity 跨電腦同步設定檔備份目錄）
>   - `scripts/`（全域環境部署與同步腳本目錄）
>   - `.obsidian/`、`.git/`（軟體工具底層隱藏目錄）
> - **業務與專案目錄（一律強制繁體中文）**：
>   - 嚴禁以純英文（如 `docs`, `output`, `scripts`, `assets`）或中英夾雜方式新建資料夾。
>   - 統一名稱範例：`素材/`、`成果/`、`工具腳本/`、`參考資料/`、`暫存草稿/`、`共用範本庫/`、`字幕逐字稿/`。

### 1.2 資料夾建立與分類檢核原則（先歸類、後建夾） ⭐⭐⭐
> [!IMPORTANT]
> **建立任何新資料夾前，必須先全面檢視現有目錄架構，優先歸納整併；嚴格禁止隨意建立零碎資料夾。**
> 
> 1. **既有目錄優先歸類**：接收到新檔案或任務時，必須先確認現有三大領域（`01_行政`、`02_教學`、`03_程式`）及其所屬子目錄中是否有可歸類的資料夾。
> 2. **全新類別評估**：只有在確定該任務或資料屬於全新類別、無法歸入現有任何既有目錄時，才允許新建資料夾存放。
> 3. **強制「編號_中文名稱」命名**：新建資料夾一律強制以「**編號_中文名稱**」格式命名（例如：`07_新專案名稱`），確保序號連續且為繁體中文。

### 1.3 臨時性檔案集中存放鐵律（99_臨時性檔案） ⭐⭐⭐
> [!IMPORTANT]
> **任務執行過程中如需產生任何臨時性檔案（包括中間過程檔、資料轉換快照、臨時腳本、除錯記錄等），一律強制統一放在「`99_臨時性檔案`」資料夾內，嚴禁散落在根目錄或正式業務目錄中，以確保工作區整潔並利於日後定期清除。**

### 1.4 協作理念與語言溝通風格
- **主要語言**：繁體中文（台灣教育部行政用語與資訊教育慣用語，例如：專案、程式碼、物件、伺服器、環境變數、模組、相容性、硬體、韌體、主旨、說明、擬辦）。
- **專業術語**：業界與學界通用專有名詞保留原文（例如：Prompt, Agent, API, Token, MCU, GPIO, Interrupt, CI/CD）。
- **回覆風格**：條理分明、由淺入深、實作導向。像熱心專業的資訊科老師與行政主管一樣，給出清楚步驟、觀念提點與安全預防。

---

## 2. 三大核心領域路由器 (Task Router)

接收到使用者需求時，Agent 依任務屬性動態路由調度對應核心領域規範：

```text
[使用者任務輸入] ───>【全域意圖路由器】
                        │
                        ├─> 🏫【校務行政】 ──> 載入 `01_行政/AGENTS.md`
                        │   (公文處理、會議流程、人事協調、法規顧問、專案補助、業務記事)
                        │
                        ├─> 🎓【教學培育】 ──> 載入 `02_教學/AGENTS.md`
                        │   (108課綱教案、實作教材講義、四級量規評量、階梯式批改引導)
                        │
                        └─> 💻【程式研發】 ──> 載入 `03_程式/AGENTS.md`
                            (電子電路、嵌入式微控制器 MCU、軟體架構設計、Clean Code、除錯 RCA)
```

---

## 3. 全域安全與隱私防護紅線 (Global Security Guardrails)

- **嚴禁金鑰硬編碼 (No Hardcoded Secrets)**：
  - 嚴格禁止在程式碼、設定檔或對話日誌中硬編碼 API Key、憑證、密碼、Token。
  - 統一使用環境變數（`.env`）管理敏感設定，並確保 `.env` 已列入 `.gitignore`。
- **高風險指令阻斷**：
  - 嚴禁未經確認執行破壞性或不可逆的操作（例如：無條件刪除系統檔案、資料庫 DROP/TRUNCATE）。
- **外部套件與依賴安全**：
  - 引入外部套件時優先選擇社群活躍、信譽良好且版本穩定的依賴。

---

## 4. 版本控制與專案歷程規範 (Git & HISTORY.md)

### 4.1 Git Commit 規範 (Conventional Commits)
提交訊息一律遵循 Conventional Commits 格式：
- `feat:` 新增功能、教材章節或模組
- `fix:` 修復問題或除錯
- `docs:` 文檔變更、教學筆記
- `style:` 格式微調（排版、標點，不影響邏輯）
- `refactor:` 程式碼或目錄結構重構
- `sync:` 跨機設定檔或 Skills 同步
- `chore:` 建置設定、依賴更新或工具鏈維護

### 4.2 專案歷程記錄規範 (HISTORY.md)
每個核心領域目錄（`01_行政`、`02_教學`、`03_程式`）均必須維護專屬之 `HISTORY.md`：
- **更新時機**：完成具里程碑意義之產出、架構重構或重大除錯後，必須即時更新對應目錄的 `HISTORY.md`。
- **單次紀錄要件**：時間序號、背景需求、實作細節/關鍵技術、異動檔案清單、驗證狀態。
